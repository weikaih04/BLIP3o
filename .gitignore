# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Training outputs
runs/
test_outputs/
wandb/

# Large data / checkpoints — committed elsewhere (anchor at project root)
/data/
/checkpoints/

# Local env
.env
.vscode/

_diag

data_viewer/

# Local evaluator dependency overlays (installed from Miniconda/pip)
third_party/eval/.deps/
third_party/eval/.deps_t3/
third_party/eval/.deps_t2i/
third_party/eval/.deps_imagereward/


tests/

# derived manifests (hundreds of MB) — regenerate with scripts/data/*.py
manifests/*.jsonl
# splits/ 里是 GB 级的 pool*_train —— 只提交生成脚本 scripts/data/mk_*.py
# (.gitignore 不支持行尾注释，写在同一行会把注释当成模式的一部分)
manifests/**/*.jsonl
manifests/DELETED_*.txt
