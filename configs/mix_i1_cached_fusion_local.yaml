# LOCAL (fsx) fusion config for ②③ — I1, cached VLM + DINO (merged mv1), fusion cond.
tasks:
  - name: image_to_3d
    weight: 1.0
    args:
      manifest: /fsx/sfr/weikaih/3dgen/data/trellis2/manifests/ready_v2/ready_v2.jsonl
      slat_resolution: 512
      crop_to_object: true
      min_aesthetic: 4.5
      cached_hidden_root: /fsx/sfr/weikaih/3dgen/data/vlm_hidden_cache/qwen35-2b_tok1024_crop_mv1
      fuse_dino: true
sampling:
  granularity: batch
  temperature: 1.0
