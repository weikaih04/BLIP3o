# Stage-2 fusion — v2.2 encoder cond FUSED with DINOv3: cond = [DINOv3 ; connector(v2.2)].
# Same offline v2.2 cache (merged v+d entries). fuse_dino: true. Pair with --dino_drop_prob 0.5
# (v2.2 is a far stronger conditioner than base Qwen → push onto v2.2, off DINO).
tasks:
  - name: image_to_3d
    weight: 1.0
    args:
      manifest: /fsx/sfr/weikaih/3dgen/data/trellis2/manifests/ready_v4_vlm_filtered/vlm_filtered_all.jsonl
      slat_resolution: 512
      crop_to_object: true
      min_aesthetic: null
      cached_hidden_root: /fsx/sfr/weikaih/3dgen/data/vlm_hidden_cache/v22_3dvlm_tok1024_mv1
      fuse_dino: true
sampling:
  granularity: batch
  temperature: 1.0
