# TEXT-ONLY S3 pilot — shape/tex stages. Single T task over the full 422k capT
# manifest, cond from the v22_tcap4 t-key cache (weighted t-sampling per its
# _meta.json: long .35 / medium .20 / short .10 / long+tex .35, t003->t000 fallback).
# Full SLAT targets: shape stage consumes target_shape_slat_512 (GT coords+feats),
# tex stage consumes target_tex_slat_512 + GT-shape concat_cond — both conditioned
# ONLY on the text cond (the plain connector path; no DINO segment exists for T).
tasks:
  - name: text_to_3d_weighted
    weight: 1.0
    args:
      manifest: /fsx/sfr/weikaih/3dgen/data/trellis2/manifests/ready_v4_vlm_filtered/vlm_filtered_capT.jsonl
      slat_resolution: 512
      cached_hidden_root: /fsx/home/weikai.huang/3dgen/vlm_hidden_cache/v22_tcap4
      ss_only: false
sampling:
  granularity: batch
  temperature: 1.0
